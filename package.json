{
    "name": "@henrotaym/laravel-temporary-files",
    "version": "1.0.0",
    "private": true,
    "license": "MIT",
    "scripts": {
        "push": "git push && git push --tags",
        "postversion": "npm run push"
    }
}
